# scan2pdf
an online tool which generates one single pdf file out of multiple (scanned) image files

scanImage()
  // input: *image*
  // output: *ScannedImage*
toPdf()
  // input: *ScannedImage*
  // output: *pdf_file*
